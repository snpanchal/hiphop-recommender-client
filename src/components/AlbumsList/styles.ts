import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
    gridContainer: {
        textAlign: 'center',
    },
    grid: {
        width: '100%',
        margin: '10px 30px',
    },
    loadingSpinner: {
        // margin: '0 auto',
    },
}));

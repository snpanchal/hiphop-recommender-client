import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
    formControl: {
        display: 'block',
        width: 200,
        marginBottom: '2rem',
    },
    select: {
        width: 200,
    },
}));
